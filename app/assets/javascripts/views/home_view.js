(function($) {
    HomeView = BaseView.extend({
        events: _.extend({
        }, BaseView.prototype.events),

        _selectors: _.extend({
        }, BaseView.prototype._selectors),

    });
}).call(this, jQuery);